<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar sesion en Uptask

            if locals.mensajes
                .alertas
                each categoria in Object.keys(locals.mensajes) 
                    each error in mensajes[categoria]
                        .alerta(class=`${categoria}`)
                            = error

            form(id="formulario" class="caja caja-login" method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu Email" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu Password" value= password)
                .campo(class="enviar")
                    input(class="boton" type="submit" value="Iniciar sesion")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aun no tienes cuenta? Crea una cuenta
                    a(href="/reestablecer") ¿Olvidaste tu contraseña?