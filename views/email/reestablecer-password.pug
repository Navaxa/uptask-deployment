<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer contraseña
        p  Hola, has solicitado reestablecer tu contraseña, has click en el siguiente enlace para reestablecerlo, este enlce es temporal, asi que si se vence vuelve a solicitarlo
        a(href=`${resetUrl}`) Ir a resetear la contraseña
        p Si no puesdes acceder al enlace visita #{resetUrl}
        p Si no solicitastes este email, puedes ignorarlo